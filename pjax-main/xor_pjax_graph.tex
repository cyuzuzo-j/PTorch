\documentclass{report}
\usepackage{tikz}
\usepackage{subcaption}

\begin{document}
\begin{figure}
  \begin{tikzpicture}[scale=1.5]
      \draw
        (0.0:2) node (mse_loss){mse_loss}
        (16.364:2) node (sum_relu){sum_relu}
        (32.727:2) node (repeat){repeat}
        (49.091:2) node (reshape){reshape}
        (65.455:2) node (relu2.bias){relu2.bias}
        (81.818:2) node (dot){dot}
        (98.182:2) node (reshape){reshape}
        (114.545:2) node (sum_relu){sum_relu}
        (130.909:2) node (repeat){repeat}
        (147.273:2) node (reshape){reshape}
        (163.636:2) node (relu1.bias){relu1.bias}
        (180.0:2) node (dot){dot}
        (196.364:2) node (repeat){repeat}
        (212.727:2) node (reshape){reshape}
        (229.091:2) node (Array(shape=(4, 2), dtype=float32)){Array(shape=(4, 2), dtype=float32)}
        (245.455:2) node (repeat){repeat}
        (261.818:2) node (reshape){reshape}
        (278.182:2) node (hidden.weight){hidden.weight}
        (294.545:2) node (repeat){repeat}
        (310.909:2) node (reshape){reshape}
        (327.273:2) node (output.weight){output.weight}
        (343.636:2) node (Array(shape=(4, 1), dtype=float32)){Array(shape=(4, 1), dtype=float32)};
      \begin{scope}[->]
        \draw (sum_relu) to (mse_loss);
        \draw (repeat) to (sum_relu);
        \draw (reshape) to (repeat);
        \draw (relu2.bias) to (reshape);
        \draw (dot) to (sum_relu);
        \draw (reshape) to (dot);
        \draw (sum_relu) to (reshape);
        \draw (repeat) to (sum_relu);
        \draw (reshape) to (repeat);
        \draw (relu1.bias) to (reshape);
        \draw (dot) to (sum_relu);
        \draw (repeat) to (dot);
        \draw (reshape) to (repeat);
        \draw (Array(shape=(4, 2), dtype=float32)) to (reshape);
        \draw (repeat) to (dot);
        \draw (reshape) to (repeat);
        \draw (hidden.weight) to (reshape);
        \draw (repeat) to (dot);
        \draw (reshape) to (repeat);
        \draw (output.weight) to (reshape);
        \draw (Array(shape=(4, 1), dtype=float32)) to (mse_loss);
      \end{scope}
    \end{tikzpicture}
\end{figure}
\end{document}