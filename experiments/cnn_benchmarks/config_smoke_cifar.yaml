experiment_name: smoke_hybrid_cifar
random_seed: 43
max_steps: 600
num_runs: 1
eval_every: 10
patience: 10000
conv_widths: [4]
batch_sizes: [64]
results_dir: results_smoke

tasks:
  - name: CIFAR10
    classes: 10
    in_channels: 3
