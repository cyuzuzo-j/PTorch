# Attention Experiment

This folder contains experimental code for a mini Vision Transformer (ViT) on MNIST.

## Guidelines Adherence

Currently, only a `ptorch` / `torch` baseline exists. For full compliance, we should eventually implement the framework parity.

## Files
- `config.yaml`: Shared configuration.
- `bench_torch.py`: Standard PyTorch implementation.
